<div class="ui two column centered row">
  <div class="column">
    <div class="ui segment">
      <p>ID: {{user.id}}</p>

      <p>Name: {{user.name}}</p>

      <p>Instagram ID:{{user.ig_id}}</p>

      <div class="ui grid">
        <div class="ui row">
          <div class="ui eight wide column">
            <a class="ui fluid blue button" href="/c3visualization">View C3 visualization</a>
          </div>
          <div class="ui eight wide column">
            <a class="ui fluid black button" href="/visualization">View D3 visualization</a>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>
